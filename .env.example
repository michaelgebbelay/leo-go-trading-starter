# GO API / GammaWizard
GW_BASE=https://gandalf.gammawizard.com
GW_TOKEN=
GW_EMAIL=
GW_PASSWORD=

# Default endpoint used by the CLI when --endpoint is omitted.
LEO_ENDPOINT=rapi/GetLeoProfit

# Broker credentials are intentionally omitted from this starter kit.
# Keep broker app keys, refresh tokens, and account numbers in your own private
# secret manager or GitHub repository secrets.
